import { PdfEngine, PdfDocument, PageData, ExtractOptions } from "./interface.js";
export declare class PdfJsEngine implements PdfEngine {
    name: string;
    private pdfiumRenderer;
    private currentPdfPath;
    private currentPdfData;
    loadDocument(input: string | Uint8Array, password?: string): Promise<PdfDocument>;
    extractPage(doc: PdfDocument, pageNum: number, options?: ExtractOptions): Promise<PageData>;
    extractAllPages(doc: PdfDocument, maxPages?: number, targetPages?: string, options?: ExtractOptions): Promise<PageData[]>;
    renderPageImage(_doc: PdfDocument, pageNum: number, dpi: number, password?: string): Promise<Buffer>;
    close(doc: PdfDocument): Promise<void>;
    private parseTargetPages;
}
//# sourceMappingURL=pdfjs.d.ts.map