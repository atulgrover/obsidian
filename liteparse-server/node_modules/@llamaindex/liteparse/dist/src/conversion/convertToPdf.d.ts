/**
 * Returns the temp directory for LiteParse operations.
 * Respects the `LITEPARSE_TMPDIR` environment variable, falling back to the OS default.
 */
export declare function getTmpDir(): string;
export interface ConversionResult {
    pdfPath: string;
    originalExtension: string;
}
export interface ConversionError {
    message: string;
    code: string;
}
export interface ConversionPassthrough {
    content: string;
}
interface ResolvedCommand {
    command: string;
    args: string[];
    resolvedPath: string;
}
export declare const officeExtensions: string[];
export declare const spreadsheetExtensions: string[];
export declare const imageExtensions: string[];
export declare const htmlExtensions: string[];
/**
 * Guess file extension from file content using file-type magic byte detection.
 * Returns the path's own extension if present, otherwise inspects file bytes.
 */
export declare function guessFileExtension(filePath: string): Promise<string | null>;
/**
 * Find LibreOffice command - handles different installation methods
 */
export declare function findLibreOfficeCommand(): Promise<string | null>;
/**
 * Find ImageMagick command - handles v6 (convert) and v7 (magick)
 */
export declare function findImageMagickCommand(): Promise<ResolvedCommand | null>;
/**
 * Convert office documents using LibreOffice
 */
export declare function convertOfficeDocument(filePath: string, outputDir: string, password?: string): Promise<string>;
/**
 * Convert images to PDF using ImageMagick
 */
export declare function convertImageToPdf(filePath: string, outputDir: string): Promise<string>;
/**
 * Main conversion function
 */
export declare function convertToPdf(filePath: string, password?: string): Promise<ConversionResult | ConversionPassthrough | ConversionError>;
/**
 * Clean up temporary conversion files
 */
export declare function cleanupConversionFiles(pdfPath: string): Promise<void>;
/**
 * Guess file extension from raw bytes using file-type magic byte detection.
 */
export declare function guessExtensionFromBuffer(data: Buffer | Uint8Array): Promise<string | null>;
/**
 * Convert a raw byte buffer to PDF by writing to a temp file and converting.
 * For PDF buffers, callers should skip this and pass data directly to the PDF engine.
 */
export declare function convertBufferToPdf(data: Buffer | Uint8Array, password?: string): Promise<ConversionResult | ConversionPassthrough | ConversionError>;
export {};
//# sourceMappingURL=convertToPdf.d.ts.map