fastapi>=0.104.0
uvicorn>=0.24.0
httpx>=0.25.0
python-frontmatter>=1.1.0
pyyaml>=6.0
python-slugify>=8.0.0
python-multipart>=0.0.6
# LLM providers (used via raw HTTP — no heavy SDK dependencies)
# anthropic and openai are called via httpx directly